# Never track secrets (shouldn't exist, but safety)
*.pem
*.key
**/credentials
**/credentials.json
**/*secret*
**/*token*

# Tool caches/state that land in config
**/cache/
**/*.log
.DS_Store
zsh/.zcompdump*
nvim/lazy-lock.json
.claude/

# Reference install of LazyVim
nvim-lazy/
